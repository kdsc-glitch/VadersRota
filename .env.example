# Database Configuration for Local Docker PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/rota_management

# If your Docker container uses different credentials, update accordingly:
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name